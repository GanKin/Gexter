---
phase: 01-shell-boundary
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/webui/compatibility.test.ts
autonomous: false
requirements:
  - WEB-09
  - WEB-10
must_haves:
  truths:
    - "现有 CLI 脚本仍指向 src/index.tsx"
    - "现有 Gateway 脚本仍指向 src/gateway/index.ts run/login"
    - "新增 WebUI 文件不会导入 gateway startup 或 cron startup"
    - "完整 typecheck 和测试套件可以在新增 WebUI 后运行"
  artifacts:
    - path: "src/webui/compatibility.test.ts"
      provides: "CLI/Gateway coexistence regression checks"
      contains: "webui does not mutate existing entrypoint scripts"
  key_links:
    - from: "package.json"
      to: "src/index.tsx"
      via: "start script"
      pattern: "\"start\": \"bun run src/index.tsx\""
    - from: "package.json"
      to: "src/gateway/index.ts"
      via: "gateway scripts"
      pattern: "\"gateway\": \"tsx src/gateway/index.ts run\""
---

<objective>
验证 CLI、Gateway 与新 WebUI 外壳并存。

Purpose: 在 Phase 1 收口时用自动回归和一次浏览器人工检查证明 WebUI 是新增入口，不破坏已有 CLI/Gateway 路径。
Output: WebUI compatibility smoke test、全量类型/测试验证，以及浏览器 shell 验证 checkpoint。
</objective>

<execution_context>
@/Users/gankin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gankin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shell-boundary/01-RESEARCH.md
@.planning/phases/01-shell-boundary/01-VALIDATION.md
@AGENTS.md
@package.json
@src/index.tsx
@src/cli.ts
@src/gateway/index.ts
@src/gateway/gateway.ts
@src/webui/runtime/adapter.ts
@src/webui/server/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compatibility regression test for entrypoints and boundaries</name>
  <files>src/webui/compatibility.test.ts</files>
  <read_first>
    src/webui/compatibility.test.ts
    package.json
    src/index.tsx
    src/cli.ts
    src/gateway/index.ts
    src/gateway/gateway.ts
    src/webui/runtime/adapter.ts
    src/webui/server/index.ts
    .planning/phases/01-shell-boundary/01-VALIDATION.md
    AGENTS.md
  </read_first>
  <behavior>
    - The test fails if `start`, `dev`, `gateway`, or `gateway:login` script values change.
    - The test fails if WebUI runtime/server files import gateway startup, cron startup, direct tool registry, scratchpad, or model provider internals.
    - The test confirms WebUI scripts exist alongside the old scripts.
  </behavior>
  <action>
    Create `src/webui/compatibility.test.ts` using `bun:test`, `node:fs`, and `node:path`.
    Read and parse `package.json` from `process.cwd()`.
    Add a test named `webui does not mutate existing entrypoint scripts` that asserts exact script values: `start` equals `bun run src/index.tsx`, `dev` equals `bun --watch run src/index.tsx`, `gateway` equals `tsx src/gateway/index.ts run`, and `gateway:login` equals `tsx src/gateway/index.ts login`.
    In the same test file, assert WebUI scripts are exact: `webui:dev` equals `vite --host 127.0.0.1`, `webui:build` equals `vite build`, `webui:preview` equals `vite preview --host 127.0.0.1`, and `webui:start` equals `bun run src/webui/server/index.ts`.
    Add a test named `webui runtime avoids gateway and core duplication imports` that reads `src/webui/runtime/adapter.ts`, `src/webui/server/index.ts`, and `src/webui/server/routes.ts` and asserts the combined text does not include `startGateway`, `startCronRunner`, `ensureHeartbeatCronJob`, `getTools`, `Scratchpad`, `callLlmWithMessages`, `../gateway/gateway`, or `../../gateway/gateway`.
    Add a test named `existing cli and gateway entry files remain present` that asserts `src/index.tsx`, `src/cli.ts`, `src/gateway/index.ts`, and `src/gateway/gateway.ts` exist.
  </action>
  <verify>
    <automated>bun test src/webui/compatibility.test.ts src/webui/runtime-adapter.test.ts src/controllers/agent-runner.test.ts src/gateway/routing/resolve-route.test.ts src/gateway/sessions/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `src/webui/compatibility.test.ts` contains `webui does not mutate existing entrypoint scripts`.
    `src/webui/compatibility.test.ts` contains `bun run src/index.tsx`.
    `src/webui/compatibility.test.ts` contains `tsx src/gateway/index.ts run`.
    `src/webui/compatibility.test.ts` contains `webui runtime avoids gateway and core duplication imports`.
    `src/webui/compatibility.test.ts` contains `startGateway`.
    `src/webui/compatibility.test.ts` contains `callLlmWithMessages`.
  </acceptance_criteria>
  <done>Compatibility test covers unchanged CLI/Gateway scripts, WebUI script additions, and WebUI boundary import restrictions.</done>
</task>

<task type="auto">
  <name>Task 2: Run full automated Phase 1 verification</name>
  <files>src/webui/compatibility.test.ts</files>
  <read_first>
    package.json
    src/webui/compatibility.test.ts
    src/webui/runtime-adapter.test.ts
    .planning/phases/01-shell-boundary/01-VALIDATION.md
    AGENTS.md
  </read_first>
  <action>
    Run the quick task-level feedback command first: `bun test src/webui/compatibility.test.ts src/webui/runtime-adapter.test.ts`.
    Then run the broader Phase 1 targeted smoke command: `bun test src/webui/compatibility.test.ts src/webui/runtime-adapter.test.ts src/controllers/agent-runner.test.ts src/gateway/routing/resolve-route.test.ts src/gateway/sessions/store.test.ts`.
    Treat `bun run typecheck && bun test` as the full-suite phase gate, not the task-level feedback command; run it only after the two targeted commands are green.
    The full command order is: `bun install`, `bun run webui:build`, quick task-level feedback, broader targeted smoke, then full-suite phase gate.
    If Bun is not available on PATH, stop and report the exact failing command `bun --version` as the environment blocker; do not switch to npm, pnpm, yarn, or node test runners because AGENTS.md requires Bun.
  </action>
  <verify>
    <automated>bun test src/webui/compatibility.test.ts src/webui/runtime-adapter.test.ts</automated>
    <phase_gate>bun run typecheck && bun test</phase_gate>
  </verify>
  <acceptance_criteria>
    `bun run webui:build` exits with status 0.
    Quick task-level feedback command `bun test src/webui/compatibility.test.ts src/webui/runtime-adapter.test.ts` exits with status 0.
    `bun test src/webui/compatibility.test.ts src/webui/runtime-adapter.test.ts src/controllers/agent-runner.test.ts src/gateway/routing/resolve-route.test.ts src/gateway/sessions/store.test.ts` exits with status 0.
    Full-suite phase gate `bun run typecheck && bun test` exits with status 0.
  </acceptance_criteria>
  <done>Full automated verification proves WebUI additions compile/test while CLI and Gateway regression checks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser shell verification</name>
  <files>package.json, src/webui/client/routes/workspace.tsx, src/webui/server/index.ts</files>
  <read_first>
    package.json
    src/webui/client/routes/workspace.tsx
    src/webui/server/index.ts
    .planning/phases/01-shell-boundary/01-VALIDATION.md
  </read_first>
  <action>
    Start the already-built Phase 1 WebUI runtime with `bun run webui:start` and the browser shell with `bun run webui:dev`, then complete the manual browser checks below. This checkpoint verifies rendered behavior only; do not edit source files during this task unless the checkpoint fails and a follow-up fix plan is created.
  </action>
  <what-built>Phase 1 WebUI shell, local runtime health endpoint, and CLI/Gateway compatibility checks.</what-built>
  <how-to-verify>
    1. In one terminal run `bun run webui:start`; expected API server listens on `127.0.0.1:5174`.
    2. In another terminal run `bun run webui:dev`; expected Vite URL is `http://127.0.0.1:5173/`.
    3. Open `http://127.0.0.1:5173/` in a browser.
    4. Confirm the first screen contains exact text `Dexter WebUI` and `Local Dexter Workspace`.
    5. Confirm the runtime status resolves to `Connected` and the displayed runtime is `dexter`.
    6. Stop both processes with Ctrl+C.
  </how-to-verify>
  <verify>
    <automated>bun run webui:build && bun test src/webui/compatibility.test.ts src/webui/runtime-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Browser page at `http://127.0.0.1:5173/` displays `Dexter WebUI`.
    Browser page at `http://127.0.0.1:5173/` displays `Local Dexter Workspace`.
    Browser page at `http://127.0.0.1:5173/` displays `Connected`.
    Direct request to `http://127.0.0.1:5174/api/runtime/health` returns JSON containing `"runtime":"dexter"`.
  </acceptance_criteria>
  <done>The user confirms the browser page opens, names Dexter WebUI, and displays connected local runtime health.</done>
  <resume-signal>Type `approved` if the browser shell matches the expected results, or describe the exact mismatch.</resume-signal>
</task>

</tasks>

<verification>
Run quick task-level feedback: `bun test src/webui/compatibility.test.ts src/webui/runtime-adapter.test.ts`.
Run broader targeted smoke: `bun run webui:build && bun test src/webui/compatibility.test.ts src/webui/runtime-adapter.test.ts src/controllers/agent-runner.test.ts src/gateway/routing/resolve-route.test.ts src/gateway/sessions/store.test.ts`.
Run full-suite phase gate: `bun run typecheck && bun test`.
Complete the blocking browser verification checkpoint.
</verification>

<success_criteria>
All Phase 1 requirements are covered: WEB-09 by unchanged CLI script and entrypoint checks, WEB-10 by unchanged Gateway script/entrypoint checks, and WebUI boundary checks by forbidden-import assertions.
Full suite `bun run typecheck && bun test` passes after WebUI files are added.
Human browser verification confirms the shell opens and reaches local runtime health.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-boundary/01-shell-boundary-03-SUMMARY.md`
</output>
