---
phase: 01-shell-boundary
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/webui/runtime/types.ts
  - src/webui/runtime/session.ts
  - src/webui/runtime/adapter.ts
  - src/webui/runtime-adapter.test.ts
  - src/webui/server/routes.ts
  - src/webui/server/index.ts
  - src/webui/client/routes/workspace.tsx
autonomous: true
requirements:
  - WEB-01
  - WEB-02
must_haves:
  truths:
    - "WebUI 有一个清晰的本地 Dexter runtime 健康检查边界"
    - "WebUI 可以创建新会话而不复制 agent/tool/model 核心逻辑"
    - "用户可以从 WebUI 首屏触发 New session 并收到 sessionId/model/status"
    - "WebUI adapter 只通过现有 Agent.create 和 agent.run 接入 Dexter 核心"
  artifacts:
    - path: "src/webui/runtime/types.ts"
      provides: "Transport-safe WebUI runtime contracts"
      contains: "RuntimeHealth"
    - path: "src/webui/runtime/adapter.ts"
      provides: "Only WebUI-to-Dexter runtime adapter"
      contains: "Agent.create"
    - path: "src/webui/server/routes.ts"
      provides: "Bun HTTP API boundary for WebUI"
      contains: "/api/runtime/sessions"
    - path: "src/webui/client/routes/workspace.tsx"
      provides: "First-screen New session action"
      contains: "POST"
    - path: "src/webui/runtime-adapter.test.ts"
      provides: "WEB-01 and WEB-02 regression tests"
      contains: "createWebRuntimeSession"
  key_links:
    - from: "src/webui/runtime/adapter.ts"
      to: "src/agent/agent.ts"
      via: "Agent.create and agent.run"
      pattern: "Agent\\.create"
    - from: "src/webui/server/routes.ts"
      to: "src/webui/runtime/adapter.ts"
      via: "getRuntimeHealth import"
      pattern: "getRuntimeHealth"
    - from: "src/webui/client/routes/workspace.tsx"
      to: "src/webui/server/routes.ts"
      via: "fetch('/api/runtime/sessions', { method: 'POST' })"
      pattern: "fetch\\('/api/runtime/sessions'.*method: 'POST'"
    - from: "src/webui/server/routes.ts"
      to: "src/webui/runtime/session.ts"
      via: "POST /api/runtime/sessions calls createWebRuntimeSession"
      pattern: "createWebRuntimeSession"
---

<objective>
定义 WebUI 与现有 Dexter 核心交互的适配边界。

Purpose: 固定 WebUI 只能通过一个窄 adapter 使用现有 Agent 运行时，防止 Phase 2 之后出现第二套 agent、tool、memory 或 gateway 逻辑。
Output: WebUI runtime 类型、会话工厂、adapter、Bun server route、首屏 New session action 和覆盖边界行为的 Bun 测试。
</objective>

<execution_context>
@/Users/gankin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gankin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shell-boundary/01-RESEARCH.md
@.planning/phases/01-shell-boundary/01-VALIDATION.md
@AGENTS.md
@src/agent/agent.ts
@src/agent/types.ts
@src/utils/config.ts
@src/utils/in-memory-chat-history.ts
@src/webui/client/routes/workspace.tsx

<interfaces>
Existing core exports to use directly:
```ts
// src/agent/agent.ts
export class Agent {
  static async create(config: AgentConfig = {}): Promise<Agent>;
  async *run(query: string, inMemoryHistory?: InMemoryChatHistory): AsyncGenerator<AgentEvent>;
}
```

```ts
// src/agent/types.ts
export interface AgentConfig {
  model?: string;
  modelProvider?: string;
  maxIterations?: number;
  signal?: AbortSignal;
  requestToolApproval?: (request: ToolApprovalEvent) => Promise<ApprovalDecision>;
  sessionApprovedTools?: Set<string>;
  messageQueue?: AsyncQueue<string>;
  channelProfile?: ChannelProfile;
}
export type AgentEvent = ThinkingEvent | ToolStartEvent | ToolEndEvent | ToolErrorEvent | ToolProgressEvent | ToolLimitEvent | ToolApprovalEvent | ToolDeniedEvent | ContextClearedEvent | MemoryRecalledEvent | MemoryFlushEvent | StreamProgressEvent | QueueDrainEvent | MicrocompactEvent | CompactionEvent | DoneEvent;
```

```ts
// src/utils/in-memory-chat-history.ts
export class InMemoryChatHistory {
  constructor(modelId: string);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WebUI runtime contracts and session model</name>
  <files>src/webui/runtime/types.ts, src/webui/runtime/session.ts, src/webui/runtime-adapter.test.ts</files>
  <read_first>
    src/webui/runtime/types.ts
    src/webui/runtime/session.ts
    src/webui/runtime-adapter.test.ts
    src/utils/in-memory-chat-history.ts
    src/agent/types.ts
    .planning/phases/01-shell-boundary/01-RESEARCH.md
    AGENTS.md
  </read_first>
  <behavior>
    - `createWebRuntimeSession('gpt-5.4')` returns a session with an id starting with `web-`.
    - The session owns an `InMemoryChatHistory` instance and `Set<string>` approved tool store.
    - Runtime event payload types import `AgentEvent` instead of redefining agent event shapes.
  </behavior>
  <action>
    Create `src/webui/runtime/types.ts` exporting `RuntimeHealth`, `WebRuntimeSession`, `WebSessionStatus`, `WebRuntimeEvent`, and `RunWebSessionOptions`.
    `RuntimeHealth` must be exactly shaped as `{ ok: boolean; runtime: 'dexter'; mode: 'webui'; model: string; gatewayCompatible: true }`.
    `WebRuntimeSession` must include `id: string`, `model: string`, `createdAt: string`, `history: InMemoryChatHistory`, `approvedTools: Set<string>`, and `status: WebSessionStatus`.
    `WebSessionStatus` must be the union `'idle' | 'running' | 'complete' | 'error'`.
    `WebRuntimeEvent` must be `{ sessionId: string; event: AgentEvent }`, importing `AgentEvent` from `../../agent/types.js`.
    `RunWebSessionOptions` must include `query: string`, `config?: AgentConfig`, and `onEvent?: (event: WebRuntimeEvent) => void | Promise<void>`.
    Create `src/webui/runtime/session.ts` exporting `createWebRuntimeSession(model = 'gpt-5.4'): WebRuntimeSession`. Use `crypto.randomUUID()` when available and prefix ids with `web-`; create `new InMemoryChatHistory(model)` and `new Set<string>()`.
    Create tests in `src/webui/runtime-adapter.test.ts` before implementation assertions pass, using `bun:test`, covering id prefix, default model `gpt-5.4`, idle status, history instance, and empty approved tool set.
  </action>
  <verify>
    <automated>bun test src/webui/runtime-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `src/webui/runtime/types.ts` contains `export type RuntimeHealth`.
    `src/webui/runtime/types.ts` contains `runtime: 'dexter'`.
    `src/webui/runtime/types.ts` contains `gatewayCompatible: true`.
    `src/webui/runtime/types.ts` contains `event: AgentEvent`.
    `src/webui/runtime/session.ts` contains `new InMemoryChatHistory(model)`.
    `src/webui/runtime/session.ts` contains `id: \`web-`.
    `src/webui/runtime-adapter.test.ts` contains `startsWith('web-')`.
  </acceptance_criteria>
  <done>Runtime contracts and session factory exist with passing colocated Bun tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Dexter runtime adapter, session route, and client action</name>
  <files>src/webui/runtime/adapter.ts, src/webui/server/routes.ts, src/webui/server/index.ts, src/webui/runtime-adapter.test.ts, src/webui/client/routes/workspace.tsx</files>
  <read_first>
    src/webui/runtime/adapter.ts
    src/webui/server/routes.ts
    src/webui/server/index.ts
    src/webui/runtime-adapter.test.ts
    src/webui/client/routes/workspace.tsx
    src/agent/agent.ts
    src/agent/types.ts
    src/utils/config.ts
    .planning/phases/01-shell-boundary/01-RESEARCH.md
    AGENTS.md
  </read_first>
  <behavior>
    - `getRuntimeHealth()` returns `{ ok: true, runtime: 'dexter', mode: 'webui', model: 'gpt-5.4', gatewayCompatible: true }` when no saved model setting exists.
    - `handleWebUiRequest(new Request('http://local/api/runtime/sessions', { method: 'POST' }))` returns status 200 and exactly `{ sessionId, model, status }`.
    - The WebUI first screen has a `New session` action that calls `fetch('/api/runtime/sessions', { method: 'POST' })`.
    - `runWebSession(session, options)` calls `Agent.create`, iterates `agent.run(query, session.history)`, forwards every event with `sessionId`, and returns the `done.answer` string.
    - `handleWebUiRequest(new Request('http://local/api/runtime/health'))` returns status 200 JSON.
  </behavior>
  <action>
    Create `src/webui/runtime/adapter.ts` importing `Agent` from `../../agent/agent.js`, `getSetting` from `../../utils/config.js`, and the WebUI types from `./types.js`.
    Export `getRuntimeHealth(): Promise<RuntimeHealth>` returning `ok: true`, `runtime: 'dexter'`, `mode: 'webui'`, `model: getSetting('modelId', 'gpt-5.4')`, and `gatewayCompatible: true`.
    Export `runWebSession(session: WebRuntimeSession, options: RunWebSessionOptions): Promise<string>`. Set `session.status = 'running'` before creating the agent, call `Agent.create({ maxIterations: 10, sessionApprovedTools: session.approvedTools, ...options.config })`, iterate `agent.run(options.query, session.history)`, call `options.onEvent?.({ sessionId: session.id, event })` for each event, store `event.answer` when `event.type === 'done'`, set `session.status = 'complete'` on success, set `session.status = 'error'` before rethrowing on error, and return the answer string.
    Do not import from `src/gateway/gateway.ts`, `src/gateway/index.ts`, `src/tools/registry.ts`, `src/agent/scratchpad.ts`, or `src/model/llm.ts` in WebUI runtime files.
    Create `src/webui/server/routes.ts` exporting `handleWebUiRequest(req: Request): Promise<Response>`; it must return `Response.json(await getRuntimeHealth())` for `GET /api/runtime/health`.
    Add `POST /api/runtime/sessions` in `handleWebUiRequest()` importing and calling `createWebRuntimeSession()` from `../runtime/session.js`; return exactly `Response.json({ sessionId: session.id, model: session.model, status: session.status })` and no extra properties. Do not duplicate agent, tool, history, or model-provider logic in this route.
    For unsupported methods on `/api/runtime/sessions`, return status 405; for unknown paths return `new Response('Not found', { status: 404 })`.
    Create `src/webui/server/index.ts` using `Bun.serve({ hostname: '127.0.0.1', port: Number(process.env.DEXTER_WEBUI_PORT ?? 5174), fetch: handleWebUiRequest })`. Do not start Gateway or cron from this server.
    Extend `src/webui/runtime-adapter.test.ts` to test `getRuntimeHealth()`, `handleWebUiRequest()` health JSON response, and `POST /api/runtime/sessions`. The session route test must assert the JSON object has exactly the keys `sessionId`, `model`, and `status`; `sessionId` starts with `web-`; `model` is `gpt-5.4` by default; and `status` is `idle`.
    Update `src/webui/client/routes/workspace.tsx` so the first screen contains a reachable `New session` button or equivalent first-screen control. Its click handler must call `fetch('/api/runtime/sessions', { method: 'POST' })`, parse the exact `{ sessionId, model, status }` response, and render the returned session id/status somewhere in the shell. Keep chat submission, streaming, persistence, and model selection out of Phase 1.
  </action>
  <verify>
    <automated>bun test src/webui/runtime-adapter.test.ts && bun run typecheck && grep -R "fetch('/api/runtime/sessions'.*method: 'POST'" src/webui/client/routes/workspace.tsx && grep -R "/api/runtime/sessions" src/webui/server/routes.ts && grep -R "createWebRuntimeSession" src/webui/server/routes.ts</automated>
  </verify>
  <acceptance_criteria>
    `src/webui/runtime/adapter.ts` contains `Agent.create`.
    `src/webui/runtime/adapter.ts` contains `agent.run(options.query, session.history)`.
    `src/webui/runtime/adapter.ts` contains `getSetting('modelId', 'gpt-5.4')`.
    `src/webui/runtime/adapter.ts` does not contain `startGateway`.
    `src/webui/runtime/adapter.ts` does not contain `getTools`.
    `src/webui/server/routes.ts` contains `/api/runtime/health`.
    `src/webui/server/routes.ts` contains `/api/runtime/sessions`.
    `src/webui/server/routes.ts` contains `createWebRuntimeSession`.
    `src/webui/server/routes.ts` contains `sessionId: session.id`.
    `src/webui/server/routes.ts` does not return extra properties from `POST /api/runtime/sessions` beyond `{ sessionId, model, status }`.
    `src/webui/client/routes/workspace.tsx` contains `New session`.
    `src/webui/client/routes/workspace.tsx` contains `fetch('/api/runtime/sessions'`.
    `src/webui/client/routes/workspace.tsx` contains `method: 'POST'`.
    `src/webui/server/index.ts` contains `DEXTER_WEBUI_PORT`.
    `src/webui/server/index.ts` contains `port: Number(process.env.DEXTER_WEBUI_PORT ?? 5174)`.
  </acceptance_criteria>
  <done>WebUI runtime boundary can report health, create a user-triggered WebUI session through `POST /api/runtime/sessions`, run sessions through existing Agent, and serve the Phase 1 API routes without gateway side effects.</done>
</task>

</tasks>

<verification>
Run `bun test src/webui/runtime-adapter.test.ts && bun run typecheck`.
Run targeted chain checks: `grep -R "/api/runtime/sessions" src/webui/server/routes.ts src/webui/client/routes/workspace.tsx`, `grep -R "createWebRuntimeSession" src/webui/server/routes.ts`, and `grep -R "fetch('/api/runtime/sessions'.*method: 'POST'" src/webui/client/routes/workspace.tsx`.
When Plan 01-01 is also complete, run `bun run webui:start`; request `http://127.0.0.1:5174/api/runtime/health` and expect JSON containing `"runtime":"dexter"` and `"mode":"webui"`, then POST `http://127.0.0.1:5174/api/runtime/sessions` and expect exactly `{ "sessionId": "...", "model": "gpt-5.4", "status": "idle" }` with no other keys.
</verification>

<success_criteria>
`src/webui/runtime/adapter.ts` is the only WebUI file that imports `Agent`.
No WebUI runtime file imports gateway startup, tool registry, scratchpad, or model provider internals.
`src/webui/client/routes/workspace.tsx` has a first-screen `New session` action wired to `POST /api/runtime/sessions`.
`src/webui/server/routes.ts` implements `POST /api/runtime/sessions` by calling `createWebRuntimeSession()` and returning exactly `{ sessionId, model, status }`.
`bun test src/webui/runtime-adapter.test.ts` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-boundary/01-shell-boundary-02-SUMMARY.md`
</output>
