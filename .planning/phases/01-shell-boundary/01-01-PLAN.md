---
phase: 01-shell-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - index.html
  - vite.config.ts
  - tsconfig.web.json
  - src/webui/client/main.tsx
  - src/webui/client/app.tsx
  - src/webui/client/routes/workspace.tsx
  - src/webui/client/styles.css
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "用户可以通过 WebUI dev server 打开浏览器入口页面"
    - "首屏明确显示 Dexter WebUI 和本地运行时连接状态"
    - "WebUI 壳层使用独立脚本启动，不改变现有 CLI/Gateway 脚本"
  artifacts:
    - path: "package.json"
      provides: "WebUI dependency and script entrypoints"
      contains: "webui:dev"
    - path: "src/webui/client/app.tsx"
      provides: "Browser application route composition"
      contains: "BrowserRouter"
    - path: "src/webui/client/routes/workspace.tsx"
      provides: "First-screen Dexter WebUI workspace shell"
      contains: "/api/runtime/health"
  key_links:
    - from: "src/webui/client/main.tsx"
      to: "src/webui/client/app.tsx"
      via: "React createRoot renders App"
      pattern: "createRoot\\(.*\\)\\.render"
    - from: "src/webui/client/routes/workspace.tsx"
      to: "/api/runtime/health"
      via: "fetch on mount"
      pattern: "fetch\\('/api/runtime/health'\\)"
---

<objective>
搭建 Phase 1 的 WebUI 浏览器外壳和基础路由。

Purpose: 让用户能打开一个真实可用的 Dexter WebUI 首屏，同时保持它只是新增入口，不替换 CLI 或 Gateway。
Output: Vite + React + React Router 的 WebUI 客户端、WebUI 专用脚本和独立 Web TypeScript 配置。
</objective>

<execution_context>
@/Users/gankin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gankin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shell-boundary/01-RESEARCH.md
@.planning/phases/01-shell-boundary/01-VALIDATION.md
@AGENTS.md
@package.json
@tsconfig.json

<interfaces>
Existing package scripts that must remain unchanged:
```json
"start": "bun run src/index.tsx",
"dev": "bun --watch run src/index.tsx",
"gateway": "tsx src/gateway/index.ts run",
"gateway:login": "tsx src/gateway/index.ts login",
"typecheck": "tsc --noEmit",
"test": "bun test"
```

The WebUI client must call this Phase 1 health endpoint, implemented by Plan 01-02:
```ts
GET /api/runtime/health
type RuntimeHealth = {
  ok: boolean;
  runtime: 'dexter';
  mode: 'webui';
  model: string;
  gatewayCompatible: true;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WebUI package scripts and build config</name>
  <files>package.json, vite.config.ts, tsconfig.web.json, index.html</files>
  <read_first>
    package.json
    tsconfig.json
    .planning/phases/01-shell-boundary/01-RESEARCH.md
    AGENTS.md
  </read_first>
  <behavior>
    - `package.json` preserves existing `start`, `dev`, `gateway`, and `gateway:login` script values exactly.
    - `package.json` adds `webui:dev`, `webui:build`, `webui:preview`, and `webui:start`.
    - `vite.config.ts` uses `@vitejs/plugin-react` and proxies `/api` to `http://127.0.0.1:5174`.
  </behavior>
  <action>
    Add runtime dependencies `react`, `react-dom`, and `react-router-dom` to `dependencies`.
    Add dev dependencies `vite`, `@vitejs/plugin-react`, `@types/react`, and `@types/react-dom` to `devDependencies`.
    Add these exact scripts to `package.json` without changing existing script values: `"webui:dev": "vite --host 127.0.0.1"`, `"webui:build": "vite build"`, `"webui:preview": "vite preview --host 127.0.0.1"`, `"webui:start": "bun run src/webui/server/index.ts"`.
    Create `vite.config.ts` exporting `defineConfig({ plugins: [react()], server: { host: '127.0.0.1', port: 5173, proxy: { '/api': 'http://127.0.0.1:5174' } }, preview: { host: '127.0.0.1', port: 4173 } })`.
    Create `tsconfig.web.json` extending `./tsconfig.json` with `compilerOptions.types` set to `["bun-types", "react", "react-dom"]`, `compilerOptions.noEmit` set to `true`, and `include` set to `["src/webui/client/**/*", "vite.config.ts"]`.
    Create `index.html` with `<div id="root"></div>` and `<script type="module" src="/src/webui/client/main.tsx"></script>`.
  </action>
  <verify>
    <automated>bun install && bun run webui:build</automated>
  </verify>
  <acceptance_criteria>
    `package.json` contains `"webui:dev": "vite --host 127.0.0.1"`.
    `package.json` contains `"webui:start": "bun run src/webui/server/index.ts"`.
    `package.json` still contains `"start": "bun run src/index.tsx"`.
    `package.json` still contains `"gateway": "tsx src/gateway/index.ts run"`.
    `vite.config.ts` contains `port: 5173`.
    `vite.config.ts` contains `'/api': 'http://127.0.0.1:5174'`.
    `index.html` contains `/src/webui/client/main.tsx`.
  </acceptance_criteria>
  <done>WebUI dependencies, scripts, Vite config, web tsconfig, and HTML entrypoint exist; `bun run webui:build` completes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Dexter WebUI shell routes</name>
  <files>src/webui/client/main.tsx, src/webui/client/app.tsx, src/webui/client/routes/workspace.tsx, src/webui/client/styles.css</files>
  <read_first>
    src/webui/client/main.tsx
    src/webui/client/app.tsx
    src/webui/client/routes/workspace.tsx
    src/webui/client/styles.css
    .planning/phases/01-shell-boundary/01-RESEARCH.md
    AGENTS.md
  </read_first>
  <behavior>
    - `/` renders a workspace route, not a marketing or landing page.
    - The first screen includes the exact text `Dexter WebUI`.
    - The workspace route fetches `/api/runtime/health` and displays one of `Connected`, `Checking`, or `Offline`.
  </behavior>
  <action>
    Create `src/webui/client/main.tsx` importing `createRoot` from `react-dom/client`, importing `App` from `./app.js`, importing `./styles.css`, and rendering `<App />` inside `document.getElementById('root')`.
    Create `src/webui/client/app.tsx` using `BrowserRouter`, `Routes`, and `Route` from `react-router-dom`; route `/` must render `WorkspaceRoute`.
    Create `src/webui/client/routes/workspace.tsx` as a typed React component. It must define `type RuntimeHealth = { ok: boolean; runtime: 'dexter'; mode: 'webui'; model: string; gatewayCompatible: true }`, call `fetch('/api/runtime/health')` in `useEffect`, and render a shell with a sidebar label `Dexter WebUI`, a main header `Local Dexter Workspace`, a disabled prompt composer placeholder `Ask Dexter about markets, filings, or companies`, and runtime status text from the health request.
    Keep Phase 1 UI scope limited to shell and boundary status only: do not implement chat submission, message streaming, session history, model selector, tool-call display, dark mode, authentication, or persistence.
    Create `src/webui/client/styles.css` with responsive shell layout, fixed readable dimensions, non-overlapping text, and no decorative landing-page hero. Use a restrained multi-color palette with CSS variables including `--surface`, `--panel`, `--border`, `--accent`, `--accent-strong`, and `--text`.
  </action>
  <verify>
    <automated>bun run webui:build</automated>
  </verify>
  <acceptance_criteria>
    `src/webui/client/app.tsx` contains `BrowserRouter`.
    `src/webui/client/app.tsx` contains `path="/"`.
    `src/webui/client/routes/workspace.tsx` contains `Dexter WebUI`.
    `src/webui/client/routes/workspace.tsx` contains `Local Dexter Workspace`.
    `src/webui/client/routes/workspace.tsx` contains `fetch('/api/runtime/health')`.
    `src/webui/client/routes/workspace.tsx` does not contain `sendMessage`.
    `src/webui/client/routes/workspace.tsx` does not contain `EventSource`.
  </acceptance_criteria>
  <done>The browser route renders a usable Phase 1 Dexter WebUI shell and builds successfully.</done>
</task>

</tasks>

<verification>
Run `bun run webui:build`.
After Plan 01-02 is complete, run `bun run webui:start` and `bun run webui:dev`, then open `http://127.0.0.1:5173/` and verify the shell reaches `/api/runtime/health`.
</verification>

<success_criteria>
`bun run webui:build` succeeds.
The repository has a `/` WebUI route whose first screen names `Dexter WebUI`.
Existing `start`, `dev`, `gateway`, and `gateway:login` scripts are byte-for-byte unchanged in `package.json`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-boundary/01-shell-boundary-01-SUMMARY.md`
</output>
